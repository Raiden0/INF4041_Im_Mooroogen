<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.esiea.im_mooroogen.application"
    android:versionCode="1"
    android:versionName="1.0">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".ObjectifActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".HistoryActivity"
            android:label="@string/title_activity_history"
            android:theme="@style/AppTheme.NoActionBar"
            android:parentActivityName=".ObjectifActivity" >
            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".ObjectifActivity" />
        </activity>
        <activity
            android:name=".AddPaidActivity"
            android:label="@string/title_activity_add_paid"
            android:theme="@style/AppTheme.NoActionBar"
            android:parentActivityName=".ObjectifActivity" >
            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".ObjectifActivity" />
        </activity>
        <activity
            android:name=".DefObjectifActivity"
            android:label="@string/title_activity_def_objectif"
            android:theme="@style/AppTheme.NoActionBar"
            android:parentActivityName=".ObjectifActivity" >
            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".ObjectifActivity" />
        </activity>
    </application>

</manifest>
